(function ($) {
	$.fn.LinkRecognition = function (options) {
		var defaults = {
			cssclass: '',
			target:'_blank'
		}

		var settings = $.extend({}, defaults, options);
		var selector = $(this);
	}
})(jQuery);